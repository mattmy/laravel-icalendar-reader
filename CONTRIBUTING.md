# Contributing

Open an issue before proposing a breaking public API change. Keep changes focused, add a Pest regression test for observable behavior, and preserve unknown iCalendar data through the generic APIs.

```bash
composer install
composer check
```

Use English for code, commits, issues, pull requests, and authoritative documentation. Update the Traditional Chinese README when changing shared user guidance.
